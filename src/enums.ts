export enum LetterSquareState {
  Default = 'default',
  Correct = 'correct',
  Present = 'present',
  Absent = 'absent',
}

export enum KeyState {
  Default = 'default',
  Correct = 'correct',
  Absent = 'absent',
}
