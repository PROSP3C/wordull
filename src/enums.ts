export enum GameState {
  Ready = 'ready',
  Playing = 'playing',
  Won = 'won',
  Lost = 'lost',
}

export enum LetterState {
  Default = 'default',
  Correct = 'correct',
  Present = 'present',
  Absent = 'absent',
}
